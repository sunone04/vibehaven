# HLD — 高层设计

高层设计文档。Agent 在此获取模块划分、依赖关系与数据边界。与 [ARCHITECTURE.md](../../ARCHITECTURE.md) 配合：后者定义分层范式，本文档登记具体模块。

## 模块划分

> 按业务域组织模块。每个模块登记其所属分层与职责。

| 模块 | 业务域 | 所属层 | 职责 | 依赖模块 |
|---|---|---|---|---|
| _待填写_ | | | | |

## 模块依赖关系

> 依赖方向须符合 [ARCHITECTURE.md](../../ARCHITECTURE.md) 的分层约束。

```
_待填写依赖图_
```

## 数据边界

> 在边界处解析数据形状（parse, don't validate），内部信任已解析类型。

| 模块 | 输入类型 | 输出类型 | 边界解析方式 |
|---|---|---|---|
| _待填写_ | | | |

## 关键流程

> 登记跨模块的关键业务流程。Agent 须理解数据如何在模块间流转。

### 流程：_待填写_

```
_待填写流程步骤_
```

## 技术约束

> 模块级技术约束（补充 [ARCHITECTURE.md](../../ARCHITECTURE.md) 的全局约束）。

- _待填写_
