# Design.md

页面设计规范。用可执行的约束描述设计意图，而非模糊的审美表述。Agent 须引用令牌而非硬编码值。

## 设计理念

> 登记产品的设计基调。例如：极简、信息密度优先、移动优先。用约束表达品味，让 Agent 能机械遵循。

_待填写_

## 设计令牌

> 将视觉决策固化为可引用的令牌。登记令牌定义文件位置（如 `tokens.css`、`tailwind.config.js`），并列出核心令牌。
> 原则：Agent 须引用令牌而非硬编码值。

- 令牌定义文件：_待填写_
- 核心令牌清单（色彩、间距、字体、圆角、阴影）：_待填写_

## 布局规范

登记：最大内容宽度、栅格列数、断点、安全区/边距。

_待填写_

## 组件规范

> 登记核心组件的设计约束。Agent 须遵循这些约束构建组件。

| 组件 | 约束 |
|---|---|
| 按钮 | _待填写_ |
| 输入框 | _待填写_ |
| 卡片 | _待填写_ |
| 导航 | _待填写_ |
| 模态/弹层 | _待填写_ |
| 表格/列表 | _待填写_ |
| 反馈/通知 | _待填写_ |

## 交互原则

1. **反馈即时**：用户操作后 _N_ms 内须有视觉反馈
2. **加载状态**：耗时超过 _N_ms 的操作须显示加载态
3. **错误处理**：错误须明确告知用户原因与可采取的下一步
4. **空状态**：列表/数据为空时须提供引导性空状态
5. **可达性**：_待填写可达性标准（如 WCAG 级别）_

## 响应式策略

| 断点 | 布局策略 |
|---|---|
| 移动端 | _待填写_ |
| 平板 | _待填写_ |
| 桌面端 | _待填写_ |

## 动效

登记：过渡时长、缓动函数、使用原则。

_待填写_

## 设计验证

> 登记用于强制执行设计约束的工具与配置位置。

- 设计令牌定义文件：_待填写_
- 组件库/Storybook：_待填写_
- 视觉回归测试：_待填写_
